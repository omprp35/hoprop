{
  "name": "telegram-render-browser-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram webhook + Playwright Chromium + custom extensions for Render",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node src/check-extensions.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.55.0"
  }
}
